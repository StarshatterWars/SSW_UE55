No F prefix 

Plain C++ class (not a UObject)

Keep Starshatter core types (Text, List, Color, etc.)

Use forward declarations aggressively to keep the header light

Add minimal Unreal includes only where necessary (e.g., FVector)

Convert all Vec3 and Point to FVector and include the proper header

Output only the given file 

remove MewmDebug.h (Unreal doesn't support it)

convert all Print type debugging to UE_LOG

Change Author to Carlos Bott

Credit John DiCamillo/ Destroyer Studios as Original Author and Studio

Use Fractal Dev Studios as the Studio

Copyright 2025-2026

Game is called Starshatter Wars

add #pragma once to all header (.h)

Element* should be SimElement*
Contact* should by SimContact*
System* should be SimSystem*
Scene* should be SimScene*
Projector* should be SimProjector*
Light* should be SimLight*
Shot* should remain SimShot*
StarSystem* should remain StarSystem*
Contact* should be SimCo0ntact*
CameraDirector* should be CameraManager*
MusicDiredctor* should be MusicManager*
Font* should be SystemFont*

add to .h files #1include "Math/Vector.h"   // FVector
add to .h files #include "Math/Color.h"    // FColor
add to .h files#include "Math/UnrealMathUtility.h" //Math

Use Unreal FColor in place of the current Color
Format all components correctly use .X, .Y and .Z for FVector

Remove any (__FILE__, __LINE__) as not supported in Unreal

apply this template to the pasted file

show complete resultant file


